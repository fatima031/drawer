import 'package:flutter/material.dart';

class favourit_view extends StatelessWidget {
  const favourit_view({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Column(
          children: [Text("Favourities")],
        ),
      ),
    );
  }
}